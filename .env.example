# Copy this file to `.env` (same directory as docker-compose.prod.yml) and
# fill in real values on the production server. `.env` is gitignored — never
# commit real secrets. docker compose automatically reads `.env` from the
# same directory, no extra flag needed.

MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=
